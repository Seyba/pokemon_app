const React = require('react')

const Index = props => {
    const myStyle = {
        color: '#ffffff',
        backgroundColor: '#000000',
    }
    return(
        <main style={myStyle}>
            <h1>See All The Pokemon!!</h1>
        </main>
    )
}

module.exports = Index